import { Box, Button } from '@mui/material';
import React from 'react';

const Cart = () => {
    return (
        <Box>
            <Button>Check out</Button>
        </Box>
    );
};

export default Cart;
